---
paths:
  - "**/*.{tsx,jsx,vue,svelte}"
  - "**/*.{css,scss,html}"
---

# デザイン規約（UI に触るとき）

上の `paths` に当たるファイルを Read した時点で読み込まれる。
ここに置くのは**従わせる規則**だけで、「なぜこの見た目か」の決定と経緯は `docs/adr/` が持つ。
現況の記述（画面構成・コンポーネントの一覧）は `docs/ARCHITECTURE.md` の領分で、ここへ複製しない。
雛形は骨格しか持たない。
見出しはリポジトリで使うものだけ残し、各項はリポジトリの初回 UI セッションで決めたことを書く。

## トークン
- 色・字体・間隔・角丸は名前付きのトークンから引く。生の値をコンポーネントに書かない
- （リポジトリのトークンの置き場と、追加するときの手順をここに書く）

## レイアウト
- （ブレークポイント・グリッド・余白の単位。リポジトリで決めた値を書く）

## コンポーネント
- 既存のコンポーネントで表せるものを新造しない。新造するなら既存との境界を一行で書く
- 状態（loading / empty / error）を持つ UI は三つを揃えて作る。正常系だけで出さない
- （リポジトリのコンポーネントの置き場と命名をここに書く）

## 文言
- UI の文言は `writing.md` の四規律に従う。記号や英略語で済ませない
- （用語集があるリポジトリはその置き場を書く）

## アクセシビリティ
- 操作はキーボードだけで完結させる。色だけで状態を伝えない
- （リポジトリで採る基準があれば書く）

## プロジェクト固有（育てる欄）
- （このプロジェクトで決めた逸脱・追加をここに追記する。理由を一行添える）
