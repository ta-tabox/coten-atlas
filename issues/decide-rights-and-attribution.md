# 引用と出典表記の方針を決める

**前提**: なし ／ **担当**: 人間 ／ **参照**: `docs/plan.md` §0 閾値3、§1、#10、S8

## 目的
この器は番組のシリーズ名・エピソードタイトル・要約を再配布する形になる。
public 化（#10）と公開（S8）の後に直すのは、ポートフォリオとして見られている最中の修正になるので、その前に線を引く。

## 作るもの（決定と文書の改訂）
- **載せるものの線引き** — シリーズ名とエピソードタイトルはそのままでよいか。
  `summary` は自分の言葉で書くか、番組の説明文を引くか
- **番組名とロゴの扱い** — ロゴは使わない方針でよいか
- **出典表記の文言と置き場所** — サイトのフッタと README の両方
- **番組公式への導線** — 配信リンクとは別に、番組そのものへ誘導する導線を必ず置くか
- `docs/plan.md` §1 へ「引用と出典」の行を追加。文言そのものの実装は S8

## 触らないもの
実装。
フッタと README を書くのは S8。

## 完了条件（機械判定）
機械判定は無い（決定と文書の改訂なので、この issue は例外）。

- `docs/plan.md` §1 に引用と出典の方針の行がある
- #10（public 化）から前提として参照できる状態になっている

## 人間の判定（別トラック）
なし。

## 登録時のラベル（提案）
`human` / `milestone`
