# シリーズの正典一覧と配信リンクを実地で確定する

**前提**: なし ／ **担当**: 人間 ／ **参照**: `docs/plan.md` §0 閾値1、§1「配信リンク」、§4

## 目的
完了条件とデータスキーマが、どちらも未検証の前提の上に乗っている。
§0 の閾値1は「公式一覧の全シリーズ」で判定すると言うが、その公式一覧が何を指すかは決まっていない（公式サイトのシリーズページか、配信基盤の番組ページか、RSS のタイトル群からの導出か）。
§1 は配信リンクを Spotify と決めたが、RSS が Spotify のエピソード URL を含むかは確認していない。
plan.md はこの確認を S6 冒頭に置いているが、`links` の形を先に固定するのは #3（スキーマ）と #6（詳細カード）なので、S6 まで待つと両方を書き直すことになる。

## 作るもの（確認して plan.md へ書き戻すもの）
- **feedUrl の実値** — `https://itunes.apple.com/lookup?id=1450522865` を実際に叩き、`feedUrl` を固定する
- **RSS 1エピソード分の生の項目** — guid の形式、`pubDate` の書式、enclosure の URL、Spotify の URL が含まれるか
- **Spotify URL が含まれない場合の代替** — 番組 URL + タイトル検索の導線で妥協するか、別の手段を採るか
- **公式シリーズ一覧の出典と総数** — 何をもって「全シリーズ」とするかを出典つきで定義し、数える
- `docs/plan.md` §1 と §4 への書き戻し

## 触らないもの
実装コード全般。
ここは調査と plan.md の改訂だけで閉じる。

## 完了条件（機械判定）
機械判定は無い（調査と文書の改訂なので、この issue は例外）。
代わりに次の二つを満たしたら close してよい。

- `docs/plan.md` §4 の feedUrl が「確認する」でなく実値になっている
- §0 閾値1 の「公式一覧」が、出典 URL と総数つきで定義されている

## 人間の判定（別トラック）
なし（全体が人間の作業）。

## 備考
このリポジトリの Claude リモートセッションは外向き HTTP が塞がれている。
`itunes.apple.com` は egress proxy に拒否されたので、ネットワークに出られる環境で回す必要がある。

## 登録時のラベル（提案）
`human` / `milestone`
