# ベースマップの利用条件と attribution を確定する

**前提**: なし ／ **担当**: 人間 ／ **参照**: `docs/plan.md` §1「ベースマップ」、#2

## 目的
#2 は OpenFreeMap の淡色スタイルを全画面に出すが、スタイル URL の実値・レート制限・帰属表示の義務はどれも未確認。
attribution は利用条件であると同時に画面に出る UI 要素なので、#2 の「作るもの」に含まれるかどうかがここで決まる。
#2 に着手してから条件を読むと、着手した後で作るものが増える。

## 作るもの（確認して plan.md へ書き戻すもの）
- **淡色スタイルの実 URL** と、キー無しで叩けること
- **利用規約上の制約** — レート制限、ポートフォリオ用途での公開の可否
- **要求される attribution** の文言と表示位置。MapLibre の `AttributionControl` の既定で足りるか
- **満たせない場合の代替** — Carto 系無料スタイルへ倒す判断基準と、そちらの attribution
- `docs/plan.md` §1「ベースマップ」の行を実 URL と attribution つきに更新する
- #2 の「作るもの」へ attribution の表示を追記する

## 触らないもの
実装コード全般。
スタイルの実装は #2 の範囲。

## 完了条件（機械判定）
機械判定は無い（調査と文書の改訂なので、この issue は例外）。

- `docs/plan.md` §1 に実 URL と attribution 文言が入っている
- #2 が attribution の表示を含む形に更新されている

## 人間の判定（別トラック）
なし。

## 登録時のラベル（提案）
`human` / `small`
