# この領域で比喩でない語を1行1語で書く。
# scripts/lint-vocabulary.sh と web/scripts/lint-comments.ts は、判定の前にここの語を検査対象の文から取り除く。
#
# 下の三つは、このリポジトリが一語で定義して使っている名前。
#   判定の口   = `web/` で打つ `pnpm check`（docs/HARNESS.md「判定の口」）
#   位置なしの印 = シリーズが代表点を持たないことを表す `anchor` の値（コードでは ANCHOR_UNLOCATED）
#   時期なしの印 = シリーズが年を持たないことを表す `timeRange` の値（コードでは TIME_RANGE_UNTIMED）
判定の口
位置なしの印
時期なしの印

# 下の一つは、禁止語の「器」を含むが、検査のスクリプトを指す一般語。
検査器
